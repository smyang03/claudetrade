# 사전등록 — 후보 family C_REGIME_V1 (국면 게이트·완만 급락) + 회피 필터 반사실 (2026-09-07)

운영자 결정(09-07 저녁): "결국 지금 전략은 못 번다는 얘기냐" → "살아남는 조건 둘을 arm으로 등록해 forward로 검증" → "진행해".
등록 시점 = 2026-09-07 17:00 KST. forward 표본은 **2026-09-08 세션부터**(`forward_start`)만 센다. 그 전은 백필(후보 생성용).

## 1. 근거 (탐색 원장 15개월, 성숙 세션, 백필 — 판정 아님)

| 조건 | n | 평균 net | 승률 | 세션 t | 반기 |
|---|---:|---:|---:|---:|---|
| KR 급락 ≤−3% × 지수 MA20 아래 | 12,816 | +2.13% | 60.6 | 2.01 | — |
| KR 급락 ≤−3% × 지수 MA20 위 | 23,685 | −1.49% | 45.4 | −2.54 | — |
| US 급락 ≤−3% × SPY MA20 아래 | 12,980 | +1.04% | 49.4 | 2.04 | — |
| US 급락 ≤−3% × SPY MA20 위 | 20,446 | −0.21% | 45.2 | −0.29 | — |
| US 5일 ≤−12% 완만하락(단일 −5% 없음), 깊은순 1 | 163 | +1.04% | 49.7 | 1.65 | H1 +1.94 / H2 +0.58 (둘 다 양수) |
| 무조건 실행(레퍼런스 필터 −5%·밴드·MAX≥8 전량) | 2,973 | +0.11% | 46.4 | −0.17 | +0.16 / +0.09 |

해석 한계: 15개월은 상승장 편중(급락 매수에 불리), 생존편향(캐시 종목), 시가 체결 가정, 국면 태그가 KR은 KOSPI 종가 이력(마지막 행 지연 가능).

## 2. 등록 arm (계약은 레퍼런스와 동일: TP12 / SL25 / D7, US BE락 4%, 비용 US 0.50 / KR 0.25)

| id | 풀 | 필터 | 진입 |
|---|---|---|---|
| c_kr_fallen_regime | xkr_fallen3 | 전일 ≤−5% & KOSPI MA20 아래(신호일) | 통과자 전량, 다음날 시가 |
| c_us_fallen_regime | xus_fallen3 | 전일 ≤−5% & SPY MA20 아래(신호일) | 통과자 전량, 다음 세션 시가 |
| c_us_slow8 | xus_slow8 | 5일 ≤−8% & 단일 −5% 없음 | 통과자 전량 |
| c_us_slow8_regime | xus_slow8 | 위 & SPY MA20 아래 | 통과자 전량 |

한도 없음(쉐도우). 실운영 환산은 원장의 `ranks.dvol_desc==1`(거래대금 1위·하루 1건) 부분집합으로 별도 계산한다. 국면 결측이면 진입하지 않는다(통과 금지).

## 3. 판정 규칙 (사전 고정)

- 표본 단위: 정산 건. 클러스터 t는 **세션 단위**(같은 날 여러 종목은 한 표본).
- 1차 판정: forward **30 정산 세션**(건이 아니라 세션) 도달 시. 통과 = 세션 t ≥ 2.0 & 평균 net > 0 & 상위 2세션 제거 후 평균 > 0 & 국면 밖 부분집합(MA20 위)과 부호 차이 유지.
- 실운영 환산 통과: 같은 기간 `dvol_desc==1` 부분집합 평균 net > 0 (n ≥ 10).
- 반증(즉시 관측 중단 아님, 표시만): forward 30세션 t < 0, 또는 국면 위/아래 차이가 사라짐, 또는 C4가 C3보다 나쁨(국면이 완만 급락엔 무의미).
- 승격 결정은 운영자만. 통과해도 canary(5만원)부터.

## 4. 회피 필터 반사실 (기존 arm에 붙였다면)

탐색 분해에서 유의했던 회피 칸: KR 급등 +5~8% 다음날(−0.86%, t −3.0), ≥+15%(−2.43%, t −6.7), KR 제약·바이오 급락/급등 다음날(−2.41 t −4.2 / −3.69 t −5.1), 돌파 중 20일 고점 대비 −20~−10%(−2.6%, t −3.5).
`tools/avoid_filter_counterfactual.py`가 레퍼런스·후보 arm의 정산 행을 "필터 적용 시 제외되었을 행"과 "남는 행"으로 갈라 두 성적을 함께 기록한다(기존 arm 계약은 바꾸지 않음). 필터 채택은 forward에서 제외 집합의 평균이 남는 집합보다 유의하게 낮을 때만.

## 5. 기록 위치
- 원장: `data/shadow/virtual_books.db` trades(strategy_id c_*), meta.filter·meta.regime·meta.ranks.
- 게이트: `virtual_gate_eval.FAMILY_OF` → C_REGIME_V1(F0와 분리, 자체 Holm).
- 대시보드 `/virtual` 스코어보드(레퍼런스 뷰)에 c_* 표시. 분해는 `discovery_breakdown` 풀별 국면 칸.

## 6. 등록 직후 백필 결과 (09-07 17:20, 성숙 세션, 판정 아님 — 등록 전 근거보다 약함)

| arm | n | 세션 | 평균 net | 승률 | 세션 t | H1 / H2 | K=1(거래대금 1위) | 상위 2세션 제외 |
|---|---:|---:|---:|---:|---:|---|---|---|
| c_kr_fallen_regime | 8,285 | 83 | +2.45% | 62.5 | 1.16 | +1.54(t 2.9) / +2.61(t 0.0) | **−1.36%** (49건) | +1.96% (t 0.7) |
| c_us_fallen_regime | 5,661 | 81 | +1.49% | 51.2 | 1.56 | +1.92 / +1.37 | +0.25% (30건) | +1.19% (t 1.1) |
| c_us_slow8 | 4,708 | 304 | +0.40% | 48.2 | 0.44 | +1.37 / −0.07 | −0.05% | +0.36% |
| c_us_slow8_regime | 2,143 | 81 | +0.12% | 44.1 | 0.87 | +2.50 / −0.64 | +0.68% | +0.04% |

솔직한 읽기:
- 국면 게이트는 평균은 양수지만 **세션 t가 1.2~1.6**으로 §1 분해(t 2.0)보다 약하다(−5% 문턱·성숙 세션·세션 단위 t). 국면 아래 세션이 15개월 중 81~83일뿐이라 표본이 몰려 있고, KR H2는 세션 간 분산이 커 t 0.04.
- **K=1(거래대금 1위) 부분집합은 KR −1.36%**: 엣지가 있다면 "풀 전체"에 있지 "가장 유동성 큰 1종목"엔 없다 — 알파=용량 재확인. 실운영 환산은 K=1이 아니라 전량 소액 분산 쪽으로 봐야 한다.
- 완만 급락은 H2에서 부호가 뒤집혀 등록 근거가 약해졌다(−12%→−8% 확장이 희석). C3·C4는 유지하되 기대치 낮춤.
- 회피 필터 반사실(c_kr_fallen_regime): 제약·바이오 제외 시 남는 행 vs 제외 행은 `data/analysis/avoid_filter_counterfactual.json`.
판정은 §3대로 forward 30 정산 세션에서만. 백필이 약해졌다고 등록을 바꾸지 않는다(사전등록 원칙).
