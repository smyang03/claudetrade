# 미검증 10층 감사 판정 (2026-07-03)

**배경**: 7/2 4개 토론이 주 퍼널을 커버한 뒤, 운영자 지시로 "안 봤거나 얕게 본" 10층 전수 감사. 토론자 2기가 월 사용한도로 사망 → 사회자 직접 실측으로 완결. 전 과정 read-only.

## 층별 판정표

| # | 층 | 판정 | 실측 근거 |
|---|---|---|---|
| 1 | **analyst mode층** (토큰 40%) | ⚠️ **기여 미입증** | KR mode 당일 지수방향 적중 46%(11/24, 결측 감안 ~52%) ≈ 동전. 실효 출력 확정: Path B 사이징 아님(`V2_FIXED_SIZING`) — SIZE_*는 **burst cap 트리거로만** 소비(pathb_runtime.py:3037), 그 외 selection 프롬프트 컨텍스트+매수차단 스위치. 최대 토큰 소비층의 측정된 기여 없음 |
| 2 | 보조 judge | ✅ 비활성(무해) | quick_exit 총 2건(마지막 6/3), buy_time_confirm raw 0건 — 물질성 0 |
| 3 | brain/교훈 총결산 | ⚠️ 측정 부재 | 승인게이트로 통제 중 + "14일 무효과" 기존 영수증. 총결산 측정은 없음(저우선 과제) |
| 4 | **오프라인 루프**(tune_*·param_tuner) | ⚠️→**정정(7/3)**: 절반만 결함 | **[정정]** tune_30~420min(토큰 3.3%)은 param_tuner가 아니라 **장중 mode 재평가**(MAINTAIN/REVERSE→rescreen 유발, trading_bot:36470 run_tuning) — **살아있는 소비자**. 죽은 소비처는 param_tuner claude_review(param_sessions 기록, Path A 전용)뿐이며 이미 세션당 ~3회 최소 빈도(토큰 0.6%, ~$0.06/일) → **빈도 축소 실익 없음, 미실행**. 최초 판정("루프 전체 죽은 소비처")은 두 루프의 혼동이었음 |
| 5 | Path A 경계 | 확정 | selection=Path B 공유 입구(플랜 생성 원천). Path A 고유(전략신호·주문경로·tuner 소비)의 현재 기여 0, 비용 소소. 존폐는 운영자 사안 |
| 6 | preopen | ✅ 가동 중 | 오늘 로그 25회·heartbeat·state 최신. 기여 측정은 부재(저우선) |
| 7 | 인프라 레이스 | ✅ 깨끗 | entry 레이스 백필 0/273, broker truth ok(7/2 18:23) |
| 8 | observed_mfe 정확성 | ✅ 대체로 정확 | 표본 6: ±0.2pp 이내 5건. 이상치 1건(F 6/25 관측 1.94 vs 분봉 0.21 — **과대**기록, 진입 전 고점 혼입 의심) — 원인 노트만 |
| 9 | **KR 거래세** | ✅ **깨끗 (중대 확인)** | 매도율 0.00195=수수료+거래세 포함(코드 명시), DB 실측 KR 왕복 0.21% 전건 일치 — **"KR 흑자" 판정 유효** |
| 10 | paper/live 드리프트 | ⚠️ 극대(현 운영 무해) | live 전용 키 253 vs 공통 78 — paper 검증 의존 결정이 현재 없어 실해 낮음. paper 재사용 시 전면 재정비 필요 |

## 라이브 검증 부산물 (감사 중 확인)
- **US 매수 재개 ✅** — RTRB 해제 첫 US 장(7/2 세션)에서 IREN 체결(23:21 진입). 통과 기준(체결≥1) 충족. 결과는 LOSS_CAP -2.64(시장 몫)
- **D2 발효 확인** — IREN에 `closed_at` 기록됨. 단 `minutes_to_close`=None **버그**: mark_closed 내 plan 조회 일시 실패 시 NameError 경로(plan 변수가 try 안에서만 정의). **정보 손실 없음**(closed_at−entry_filled_at로 재계산 가능). 수정은 장중 편집 금지 수칙에 따라 US 마감(05:00) 후

## 개선안 (운영자 결정 대기)

| 우선 | 안 | 성격 |
|---|---|---|
| 1 | **minutes_to_close 버그 수정** (plan 변수 try 밖 초기화+재조회 fallback) | 코드 소수정, 마감 후 |
| 2 | **analyst층 비용·기여 정합** — (a) mode 적중률 누적 측정 도구(지수 데이터 결측 처리 포함) (b) 토큰 리뷰 L3와 병합: R2 조건부 실행/빈도 축소 검토 — 40% 토큰의 근거 마련 | (a)측정 (b)품질 질문=운영자 |
| 3 | ~~tune_* 루프 처분~~ **[정정으로 철회]** — tune_*=살아있는 mode 재평가, param_tuner=이미 최소 빈도. 축소 대상 부재 | 미실행 |
| 4 | 저우선 측정: brain 총결산·preopen 기여·MFE 이상치(F 6/25) 원인 | 측정 |
| — | 비이슈 확정: KR 거래세·레이스·보조 judge·paper 드리프트(현 운영) | 없음 |

## 한계
- mode 적중률은 n=24+지수 결측 2~3건 — 방향성 신호이지 확정 아님. 층 1·3·6의 "기여 측정"은 부재 확인까지이며 기여 부정의 증명이 아님.
- 토론 형식이 한도로 중단돼 층 1·4의 반대 논증(물질성 방어)은 사회자가 자체 수행 — 통상 대비 검증 강도 낮음.
